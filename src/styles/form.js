import { StyleSheet } from "react-native";
import colors from "./colors";

const fornStyles = StyleSheet.create({
    input: {
        marginBottom: 20
    },
    btnSucces: {
        padding: 5,
        backgroundColor: "#050400",
    },
    btnText: {
        marginTop: 10
    },
    btnTextLabel: {
        color: colors.dark,
    },

});

export default fornStyles;